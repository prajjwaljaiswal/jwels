NEXT_PUBLIC_API_URL=http://localhost:4000

# Subdomain root used to display a vendor's storefront URL ({slug}.<this>) in the
# Domains settings tab. Should match the storefront's NEXT_PUBLIC_APP_DOMAIN.
NEXT_PUBLIC_APP_DOMAIN=store.vrindaonline.com
